import 'package:flutter/material.dart';

class AppColors {
  // Palette minimaliste noir/blanc/gris
  static const primary = Color(0xFF000000);        // Noir pur
  static const secondary = Color(0xFF333333);      // Gris très foncé
  static const accent = Color(0xFF666666);         // Gris moyen
  static const error = Color(0xFF1A1A1A);          // Noir légèrement gris
  static const warning = Color(0xFF4D4D4D);        // Gris foncé
  static const success = Color(0xFF808080);        // Gris neutre

  static const background = Color(0xFFFFFFFF);     // Blanc pur
  static const surface = Color(0xFFF5F5F5);        // Gris très clair
  static const textPrimary = Color(0xFF000000);    // Noir
  static const textSecondary = Color(0xFF999999);  // Gris clair

  // Bordures et séparateurs
  static const border = Color(0xFFE0E0E0);         // Gris très clair pour bordures
  static const divider = Color(0xFFF0F0F0);        // Gris ultra clair
}
